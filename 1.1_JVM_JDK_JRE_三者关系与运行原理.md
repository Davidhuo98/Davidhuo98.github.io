# 1.1 JVM、JDK、JRE 三者关系与运行原理

> 重要性：★★★★★  
> 关键词：字节码 .class、类加载机制、JIT 编译、JVM 规范与实现

---

## 一、三者定义与包含关系

```
JDK（Java Development Kit）
│
├── JRE（Java Runtime Environment）
│   │
│   ├── JVM（Java Virtual Machine）
│   │     ├── 类加载子系统
│   │     ├── 运行时数据区
│   │     ├── 执行引擎（解释器 + JIT）
│   │     └── 垃圾回收器
│   │
│   └── 核心类库（rt.jar / java.base 模块）
│
├── 开发工具
│     ├── javac（编译器）
│     ├── javap（反编译器）
│     ├── jdb（调试器）
│     ├── jconsole / jvisualvm（监控）
│     └── jar / javadoc 等
```

| 层次 | 全称 | 作用 | 使用场景 |
|------|------|------|----------|
| **JVM** | Java Virtual Machine | 执行字节码，屏蔽操作系统差异 | 运行任何 Java 程序的核心 |
| **JRE** | Java Runtime Environment | JVM + 核心类库，提供运行环境 | 仅需运行 Java 程序（不开发） |
| **JDK** | Java Development Kit | JRE + 开发工具链，提供完整开发套件 | 编写、编译、调试 Java 程序 |

> 📌 **记忆口诀**：JDK 包含 JRE，JRE 包含 JVM。开发装 JDK，生产只装 JRE（或 JDK）。
> 
> ⚠️ **JDK 9+ 变化**：JRE 不再作为独立目录存在，官方推荐使用 `jlink` 自定义运行时镜像。

---

## 二、Java 程序完整执行流程

```
源代码 .java
    │
    │  javac 编译
    ▼
字节码 .class（平台无关的中间表示）
    │
    │  JVM 加载
    ▼
┌─────────────────────────────────────────┐
│              JVM 内部                    │
│                                         │
│  1. 类加载子系统                         │
│     Loading → Linking → Initialization │
│                                         │
│  2. 运行时数据区                         │
│     堆 / 栈 / 方法区 / PC寄存器          │
│                                         │
│  3. 执行引擎                             │
│     解释执行 → 热点检测 → JIT 编译       │
│                                         │
└─────────────────────────────────────────┘
    │
    ▼
机器码（本地 CPU 执行）
```

### 2.1 编译阶段（javac）

```java
// 源代码：HelloWorld.java
public class HelloWorld {
    public static void main(String[] args) {
        System.out.println("Hello, World!");
    }
}
```

```bash
# 编译生成 HelloWorld.class
javac HelloWorld.java

# 查看字节码（反编译）
javap -c HelloWorld.class
```

字节码示例（`javap -c` 输出片段）：
```
public static void main(java.lang.String[]);
  Code:
     0: getstatic     #7   // Field java/lang/System.out
     3: ldc           #13  // String Hello, World!
     5: invokevirtual #15  // Method println
     8: return
```

> 字节码是与平台无关的中间代码，由 JVM 负责在目标平台上执行，这正是 Java **"一次编写，到处运行"** 的根本原因。

---

## 三、类加载机制（Class Loading）

### 3.1 类加载的五个阶段

```
Loading（加载）
    │   将 .class 文件二进制数据读入内存
    ▼
Verification（验证）
    │   校验字节码格式合法性、安全性
    ▼
Preparation（准备）
    │   为静态变量分配内存，赋默认值（0/null/false）
    ▼
Resolution（解析）
    │   将符号引用替换为直接引用
    ▼
Initialization（初始化）
        执行 <clinit>()，运行静态初始化块和静态变量赋值
```

**关键示例：准备 vs 初始化的区别**

```java
public class Demo {
    // 准备阶段：value = 0（默认值）
    // 初始化阶段：value = 100（显式赋值）
    static int value = 100;

    // 常量在准备阶段就直接赋值为 666（编译时常量）
    static final int CONST = 666;
}
```

### 3.2 双亲委派模型（Parents Delegation Model）

```
自定义类加载器（Custom ClassLoader）
        │  委派给父加载器
        ▼
应用类加载器（Application ClassLoader）
        │  委派给父加载器
        ▼
扩展类加载器（Extension ClassLoader）
        │  委派给父加载器
        ▼
启动类加载器（Bootstrap ClassLoader）
        │  尝试加载，若无法加载
        │  则逐级返回子加载器
        ▼
    加载成功或抛出 ClassNotFoundException
```

**双亲委派的作用**：
- **安全性**：防止核心 API 被用户代码篡改（如用户自定义 `java.lang.String` 无法生效）
- **唯一性**：同一个类只会被加载一次，避免类的重复加载

**打破双亲委派的场景**：

| 场景 | 说明 |
|------|------|
| JDBC / SPI 机制 | 启动类加载器需要加载用户实现的驱动，使用线程上下文类加载器 |
| Tomcat 多 Web 应用 | 每个 WebApp 有独立类加载器，实现类隔离 |
| OSGi 热部署 | 模块化框架，类加载器形成网状结构 |

---

## 四、JIT 即时编译（Just-In-Time Compilation）

### 4.1 解释执行 vs JIT 编译

```
字节码指令
    │
    ├──[解释器 Interpreter]──→  逐条翻译执行，启动快，执行慢
    │
    └──[JIT 编译器]─────────→  热点代码编译为机器码，执行快，有编译开销
```

### 4.2 热点检测机制

JVM 通过**计数器**识别热点代码：

| 计数器类型 | 触发条件 | 阈值（默认，server 模式） |
|-----------|---------|------------------------|
| 方法调用计数器 | 方法被调用次数超过阈值 | 10000 次 |
| 回边计数器 | 循环体执行次数超过阈值 | 触发 OSR（栈上替换） |

### 4.3 JIT 编译器分级（JDK 7+）

```
Level 0：解释执行（无性能信息）
Level 1：C1 编译（简单优化，快速编译）
Level 2：C1 编译（有方法调用次数信息）
Level 3：C1 编译（有完整 profile 信息）
Level 4：C2 编译（深度优化，耗时较长）
```

**C1 vs C2 编译器对比**：

| 维度 | C1（Client 编译器） | C2（Server 编译器） |
|------|-------------------|-------------------|
| 编译速度 | 快 | 慢 |
| 优化程度 | 轻度 | 深度（内联、逃逸分析等） |
| 适用场景 | 启动敏感、交互式应用 | 长期运行的服务端应用 |

### 4.4 JIT 关键优化技术

```java
// 1. 方法内联（Inlining）
// 编译前：
int add(int a, int b) { return a + b; }
int result = add(1, 2);

// 内联后（消除方法调用开销）：
int result = 1 + 2;
```

```java
// 2. 逃逸分析（Escape Analysis）
// 对象不逃出方法范围 → 栈上分配（无 GC 压力）
void process() {
    // Point 对象不逃逸，JIT 可在栈上分配
    Point p = new Point(1, 2);
    System.out.println(p.x + p.y);
}
```

---

## 五、JVM 规范与主流实现

### 5.1 JVM 规范

《Java 虚拟机规范》（The Java Virtual Machine Specification）由 Oracle 维护，定义了：
- 字节码指令集
- 运行时数据区结构
- 类文件格式
- 类加载语义

**规范与实现是分离的**，任何语言只要能编译为符合规范的字节码，均可在 JVM 上运行。

### 5.2 主流 JVM 实现对比

| JVM 实现 | 维护方 | 特点 |
|----------|-------|------|
| **HotSpot** | Oracle | 最主流，JDK 默认，C2 深度优化 |
| **OpenJ9** | IBM/Eclipse | 低内存占用，适合容器化部署 |
| **GraalVM** | Oracle | AOT 原生编译，极速启动 |
| **Azul Zing** | Azul Systems | 无停顿 GC，低延迟场景 |

### 5.3 其他 JVM 语言

```
Kotlin    → 编译为 .class → 运行在 JVM
Scala     → 编译为 .class → 运行在 JVM
Groovy    → 编译为 .class → 运行在 JVM
Clojure   → 编译为 .class → 运行在 JVM
```

---

## 六、面试高频考题

**Q1：JVM、JDK、JRE 的区别？**

> JVM 是执行字节码的虚拟机；JRE = JVM + 核心类库，是运行 Java 程序的最小环境；JDK = JRE + 开发工具（javac 等），是完整开发套件。三者是包含关系：JDK ⊃ JRE ⊃ JVM。

**Q2：为什么 Java 能跨平台？**

> 源代码通过 javac 编译为平台无关的字节码（.class），JVM 负责将字节码翻译为目标平台的机器码。跨平台的本质是：字节码统一，各平台有对应的 JVM 实现。

**Q3：什么是双亲委派模型？为什么需要它？**

> 类加载时先委派给父加载器，父加载器无法加载时才由子加载器尝试加载。好处：防止核心 API 被用户代码覆盖（安全性），保证同一类只加载一次（唯一性）。

**Q4：JIT 编译与解释执行的区别？**

> 解释执行逐条翻译字节码，启动快但执行慢；JIT 将热点代码编译为本地机器码缓存，执行快但有编译开销。JVM 混合使用两种方式，启动用解释器，热点代码用 JIT 优化。

**Q5：类加载的五个阶段分别做什么？**

> Loading（读入字节流）→ Verification（验证合法性）→ Preparation（静态变量赋默认值）→ Resolution（符号引用转直接引用）→ Initialization（执行静态初始化代码）。

---

## 七、知识点总结

```
JVM / JDK / JRE
├── 包含关系：JDK ⊃ JRE ⊃ JVM
├── 跨平台原理：源码 → 字节码（javac）→ 机器码（JVM）
│
├── 类加载机制
│   ├── 五阶段：Loading / Verification / Preparation / Resolution / Initialization
│   └── 双亲委派：安全性 + 唯一性，JDBC/SPI/Tomcat 会打破
│
├── 执行引擎
│   ├── 解释器：逐条解释，启动快
│   └── JIT：热点检测 → 机器码缓存，C1（快速）+ C2（深度优化）
│
└── JVM 规范与实现分离
    ├── HotSpot（主流）/ OpenJ9（低内存）/ GraalVM（AOT）
    └── Kotlin / Scala / Groovy 等语言也运行在 JVM 上
```

---

*下一节 → [1.2 基础数据类型与包装类、自动装箱拆箱](./1.2_基础数据类型与包装类_自动装箱拆箱.md)*
